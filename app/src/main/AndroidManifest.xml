<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/cooking_reciept_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cooking_reciept_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ITI_Project"
        tools:targetApi="31">
        <activity
            android:name=".ui.RecipeActivity.main.RecipeActivity"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" >


    </activity>
        <activity
            android:name=".ui.actitvity1.mainActitvity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>